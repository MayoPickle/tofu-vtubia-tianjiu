# 环境变量示例文件
# 复制此文件为 .env 并设置适当的值

# Flask 环境: development, production 或 testing
FLASK_ENV=development

# 应用密钥 (用于会话加密)
SECRET_KEY=your_secret_key_here

# 数据库路径
DB_PATH=songs.db

# Debug 模式 (仅在开发环境中为 true)
FLASK_DEBUG=true

# 主机和端口
HOST=0.0.0.0
PORT=5000 